{
  "name": "raythm-web",
  "private": true,
  "scripts": {
    "dev:submissions": "wrangler pages dev",
    "db:migrate:local": "wrangler d1 migrations apply DB --local"
  },
  "devDependencies": {
    "wrangler": "^4.26.0"
  }
}
